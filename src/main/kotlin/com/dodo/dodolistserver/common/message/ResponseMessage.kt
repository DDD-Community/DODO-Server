package com.dodo.dodolistserver.common.message

class ResponseMessage {
    companion object {
        val SUCCESS = "success"
        val FAILURE = "failure"
        val LOGIN = "login"
        val JOIN = "join"
        val UPDATE = "update"
        val DELETED = "deleted"
        val SUBMITTED = "submitted"
    }
}